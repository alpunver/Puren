<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex align-items-center justify-content-center" style="min-height: 100vh; background-color: #f8f9fa;">
    <div class="container">
        <div class="card shadow-lg mx-auto border-0" style="max-width: 500px; border-radius: 15px;">
            <div class="card-body p-5 text-center">
                <h2 class="card-title fw-bold mb-3"><%= product.name %></h2>
                <h1 class="display-3 text-primary fw-bold my-4"><%= product.price %> <span class="fs-4 text-muted">TL</span></h1>
                <div class="alert alert-light border">
                    <p class="mb-0 lead"><%= product.description ? product.description : 'Açıklama girilmemiş.' %></p>
                </div>
                <a href="/" class="btn btn-outline-dark mt-4 px-4 rounded-pill">← Listeye Dön</a>
            </div>
        </div>
    </div>
</body>
</html>